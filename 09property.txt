class 클래스이름
{
	데이터형식 필드이름
	접근한정자 데이터형식 프로퍼티이름
	{
	get
	{
		return 필드이름;
	}

	set
	{
		필드이름 = value;
	}
}
}
//123
프로퍼티 선언 문법에서 get{}과 set {}을 일컬어 접근자(accessor)라고 합니다.
get 접근자는 필드로부터 값을 읽어오고
set 접근자는 필드에 값을 할당합니다.

무명 형식은 값 변경 불가

변수이름.GetType()

결과: System.Int32

